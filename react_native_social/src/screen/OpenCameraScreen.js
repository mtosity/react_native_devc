import React from 'react'
import { View, Text } from 'react-native'

const OpenCamera = () => {
    return (
        <View style={{flex: 1, justifyContent: 'center', alignItems: 'center'}}>
            <Text>Sorry, not develop yet!</Text>
        </View>
    )
}

export default OpenCamera;
